name: ctp500-receipt-printer

services:
  receipt-app:
    build:
      context: .
      dockerfile: Dockerfile
    image: ctp500-receipt-printer:latest
    ports:
      - "8080:80"
    restart: unless-stopped
