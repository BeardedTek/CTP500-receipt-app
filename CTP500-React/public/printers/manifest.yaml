# Optional. When the server exposes JSON for GET /printers/ (nginx autoindex, or Vite dev), every
# *.yaml here except this file is loaded automatically — you do not need a printer list in this file.
#
# Still useful for:
#   - extra_optional_services: extra GATT UUIDs for Web Bluetooth
#   - printers: fallback list if /printers/ is not JSON (no directory index)

extra_optional_services: []
